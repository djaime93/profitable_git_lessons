# git  init
# git add .
# git commit -m "your message goes here"
# git status
# git log (hit 'q' to exit)
# git chackout (unique commit id)

Git Remote Commmands
# git remote add origin (url)
# git remote -v
# git push origin master
# git pull origin master